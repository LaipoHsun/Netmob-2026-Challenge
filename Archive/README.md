# Archive

This directory contains superseded or mixed-stage material retained for provenance. Nothing under `Archive/` is part of the canonical staged pipeline, and current code must not import or read it.

See `legacy_pipeline/README.md` for a file-by-file description.
