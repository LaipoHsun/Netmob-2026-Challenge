# Output

Only final analysis artifacts belong here:

- `Figure/`: publication figures.
- `Table/`: statistical and publication tables.
- `overleaf/`: LaTeX sources.
- `Results_pdf/`: compiled manuscripts and reports.

Reusable datasets belong in `data_after_all_processed/`, not in `Output/`.
