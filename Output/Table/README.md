# Tables

Statistical summaries, model coefficients, bootstrap results, and publication-ready table data generated by `Analysis_code/` belong here.

Reusable feature/index tables do not belong here; those are stored in `data_after_all_processed/`.
